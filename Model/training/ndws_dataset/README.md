# NDWS Dataset Training Pipeline

This directory is reserved for experimenting with and training on the Next Day Wildfire Spread (NDWD/NDWS) dataset.

Since NDWD provides data as isolated 64x64 TensorFlow Record crops rather than a continuous NetCDF map, the training scripts here will differ significantly from the `custom_dataset` folder.